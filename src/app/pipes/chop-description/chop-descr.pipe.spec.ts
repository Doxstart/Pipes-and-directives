import { ChopDescrPipe } from './chop-descr.pipe';

describe('ChopDescrPipe', () => {
  it('create an instance', () => {
    const pipe = new ChopDescrPipe();
    expect(pipe).toBeTruthy();
  });
});
